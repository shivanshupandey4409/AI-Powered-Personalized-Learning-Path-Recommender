# Security note

The credentials/API key included in the uploaded document were intentionally NOT copied into this project.

Because secrets were shared in a document/chat, rotate the Gemini key and MongoDB password before using them in a real environment.

Put new values only in `server/.env`.
